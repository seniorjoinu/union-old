type Error = variant {
    InsufficientBalance;
    AccessDenied;
}

service : {
    "balance_of": (principal, opt int64) -> (nat) query;
    "mint": (principal, nat) -> (opt Error);
    "send": (principal, nat) -> (opt Error);
    "burn": (nat) -> (opt Error);
}
